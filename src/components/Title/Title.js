import React from 'react'

const Title = ({Title}) => {
    return (
        <div className="title">
            <h1>
                {Title}
            </h1>
        </div>
      );
}
 
export default Title;