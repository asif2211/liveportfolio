import React from 'react'

const SubTitle = ({sub_title}) => {
    return (
        <div className="sub-title">
            <h1>
                {sub_title}
            </h1>
        </div>
      );
}
 
export default SubTitle;